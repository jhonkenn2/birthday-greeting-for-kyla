# Interactive Birthday Greeting Cake with Card

A cute and colorful interactive birthday greeting webpage for Kyla.

## Run

Open `index.html` in a browser.

## Test

```bash
npm test
```

On Windows PowerShell, use:

```powershell
npm.cmd test
```

The app has no external dependencies.
